[project]
name = "ai-news-roundup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ddgs>=9.15.0",
    "mcp>=2.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
